# AI-IoT Vision Control System

Proyek lengkap sistem AI untuk deteksi objek dengan YOLO, OCR, dan kontrol servo otomatis.

## Quick Start

1. Upload `esp32_cam.ino` ke ESP32-CAM
2. Upload `esp32_servo_controller.ino` ke ESP32
3. Install dependencies: `pip install -r requirements.txt`
4. Update IP addresses di semua file
5. Run server: `python app.py`
6. Buka: http://localhost:5000

## Files app.py, index.html, dashboard.js

Silakan copy dari artifacts di chat Claude!
